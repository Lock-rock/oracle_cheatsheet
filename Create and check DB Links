CHECK ALL DBLINKS:

SELECT DB_LINK, USERNAME, HOST FROM ALL_DB_LINKS;

CREATE DBLINK:

CREATE PUBLIC DATABASE LINK  iditpreprod1 CONNECT TO idit_ro IDENTIFIED BY IDIT_RO USING '(DESCRIPTION= (ADDRESS= (PROTOCOL=TCP)(HOST=10.10.2.150) (PORT=1521))(CONNECT_DATA=(SID=IDITDMPREPRO)))';

or
CREATE PUBLIC DATABASE LINK "BIOMP"
CONNECT TO SYSTEM IDENTIFIED BY sys
USING 'BIOMP';


and add in tns entery in dblink database:

BIOMP =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = 10.10.1.189)(PORT = 1531))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SID = BIOMP)
    )
  )

conform its work check:

select * from dual@BIOMP;  

DROP DBLINK:

drop DATABASE LINK IDITPREPROD;
