modification in server:

SELECT *
FROM (
    SELECT SNAP_ID, PARAMETER_NAME, VALUE, ISDEFAULT, ISMODIFIED,
           DBID, INSTANCE_NUMBER, CON_ID
    FROM DBA_HIST_PARAMETER
    WHERE PARAMETER_NAME IS NOT NULL
    ORDER BY SNAP_ID DESC
)
WHERE ROWNUM <= 50;
